# Write Like Me

When the user asks you to write in their voice, run `wlm context` and follow the returned constraints. Preserve meaning and facts, treat patterns as tendencies, and never copy secrets or claim to be the user. If the profile is empty, ask for representative writing samples and capture them with `wlm capture` only after the user agrees.
